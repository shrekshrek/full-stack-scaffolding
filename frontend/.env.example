    # Example environment variables for the frontend application
    # For local development, create a .env.development.local file and override these if needed.
    # For production builds, these values (especially VITE_API_BASE_URL) might be set by your CI/CD environment.

    VITE_APP_NAME="My Fullstack App"
    VITE_API_BASE_URL="/api/v1" # Default for local dev if .env.development is not used

    # Add other variables your application might need:
    # VITE_SOME_OTHER_KEY="some_default_value"