// This file can be used for global TypeScript type definitions
// that are shared across the entire frontend application.

// Example: A generic API response structure
// export interface ApiResponse<T = any> {
//   success: boolean;
//   data: T;
//   message?: string;
//   errorCode?: number;
// }

// Example: User type (can also be in features/auth/types.ts or features/user/types.ts)
// export interface User {
//   id: string | number;
//   username: string;
//   email: string;
//   avatarUrl?: string;
//   // Add other user-related fields
// }

// It's often better to keep feature-specific types within their respective feature modules.
// Use this file for truly global types or interfaces that don't belong to a specific feature.

// console.log('Global types file loaded.'); 